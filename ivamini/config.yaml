environment: dev

logging:
  enabled: true
  level: INFO

permissions:
  default: READ
  allow_destructive: false

voice:
  enabled: true
  require_mode: true

llm:
  backend: disabled
  model: "none"

workspace:
  path: ivamini/workspace
